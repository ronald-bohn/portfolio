#include <string.h>

extern int replaceAll(char me, char with, char* in);
extern int replace(char me, char with, char* in);
extern int firstIndexOf(char me, char* in);
extern int lastIndexOf(char me, char* in);
